'use strict';

// used for main component and graph widget component,
// as firefox can't access the root div to get the actual padding
export const pageContentPadding = 22;
export const pageBottomPaddingForEmbeddingResource = 50;
